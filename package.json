{
  "name": "appointment-notification-backend",
  "version": "1.0.0",
  "description": "Backend for appointment notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no tests specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.11.0",
    "node-cron": "^3.0.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
